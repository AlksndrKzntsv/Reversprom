import base64
import os
from pathlib import Path

from dotenv import load_dotenv
from openai import OpenAI


BASE_PROMPT = """Разработай современный профессиональный логотип для российской промышленной компании «РЕВЕРСПРОМ».

Компания занимается реверс-инжинирингом, восстановлением технических чертежей, проектированием и производством металлических деталей по образцу.

ЗАДАЧА:
Создай настоящий корпоративный фирменный логотип, который выглядит как результат работы профессионального бренд-дизайнера, а не как иллюстрация или 3D-рендер.

Название компании:
«РЕВЕРСПРОМ»

ОБЯЗАТЕЛЬНО:
- текст должен быть написан именно русскими буквами;
- слово «РЕВЕРСПРОМ» должно быть полностью правильным и легко читаемым;
- не использовать латинские буквы;
- не добавлять никаких дополнительных слов;
- буквы должны иметь единый профессиональный шрифт;
- логотип должен выглядеть убедительно в маленьком размере.

КОНЦЕПЦИЯ:
Создай простой запоминающийся знак, связанный с реверс-инжинирингом и промышленным производством.

Основная идея знака — преобразование технической информации в готовую деталь:
чертёж → инженерия → производство.

Можно использовать абстрактную геометрическую форму, построенную из линий технического чертежа, профиля детали, сечения механизма или стилизованной буквы «Р».

Допускается очень аккуратный намёк на шестерню или механический элемент, но он должен быть частью единого знака, а не отдельной картинкой.

НЕ нужно буквально изображать завод, станок, гайки, болты, инструменты или множество шестерён.

СТИЛЬ:
- минималистичный;
- современный;
- инженерный;
- технологичный;
- премиальный B2B;
- строгий;
- уверенный;
- геометричный;
- легко узнаваемый.

ВАЖНО:
Это ПЛОСКИЙ ФИРМЕННЫЙ ЗНАК.

Не создавать реалистичный металлический объект.
Не создавать 3D-рендер.
Не использовать фотореалистичные материалы.
Не использовать сильные отражения.
Не использовать объёмные металлические буквы.
Не использовать сложные тени.
Не использовать декоративный фон.
Не использовать чрезмерное количество мелких деталей.

Используй чистые геометрические формы, чёткие линии и профессиональную типографику.

ЦВЕТА:
Основные:
- глубокий тёмно-синий;
- графитовый;
- белый.

Допускается небольшой акцент холодного серебристого или бронзового цвета.

Цветов должно быть немного.

КОМПОЗИЦИЯ:
Знак + название «РЕВЕРСПРОМ».

Знак должен хорошо работать отдельно от названия.

Название должно быть визуально сбалансировано со знаком.

Создай несколько визуальных решений внутри одной профессиональной концепции, но не превращай изображение в коллекцию случайных логотипов.

Логотип должен подходить для:
официального сайта компании, технической документации, чертежей, коммерческих предложений, визиток, одежды сотрудников и промышленной продукции.

Итоговое ощущение:
точность, инженерная экспертиза, производство, надёжность и современная технология.

Создай логотип, который мог бы реально использовать российский инженерно-производственный бренд."""

OUT_DIR = Path(__file__).resolve().parent

JOBS = [
    {
        "filename": "logo_reversprom_horizontal_white.png",
        "size": "1536x1024",
        "prompt_suffix": "Сделай горизонтальную композицию. Фон: чистый белый (#FFFFFF).",
    },
    {
        "filename": "logo_reversprom_horizontal_dark.png",
        "size": "1536x1024",
        "prompt_suffix": "Сделай горизонтальную композицию. Фон: тёмный (графитовый/почти чёрный, например #0B1220). Сделай инвертированную версию, чтобы логотип был контрастным и читаемым на тёмном фоне.",
    },
    {
        "filename": "logo_reversprom_square_white.png",
        "size": "1024x1024",
        "prompt_suffix": "Сделай квадратную композицию (1:1). Фон: чистый белый (#FFFFFF).",
    },
    {
        "filename": "logo_reversprom_square_dark.png",
        "size": "1024x1024",
        "prompt_suffix": "Сделай квадратную композицию (1:1). Фон: тёмный (графитовый/почти чёрный, например #0B1220). Сделай инвертированную версию, чтобы логотип был контрастным и читаемым на тёмном фоне.",
    },
]


def _build_prompt(suffix: str) -> str:
    return f"{BASE_PROMPT}\n\nДополнительно:\n{suffix}\n"


def main() -> None:
    # Load OPENAI_API_KEY from .env in the project folder
    load_dotenv(OUT_DIR / ".env")

    api_key = os.getenv("OPENAI_API_KEY")
    if not api_key:
        raise SystemExit(
            "OPENAI_API_KEY not found. Create /Users/andrey/Reversprom/.env with:\n"
            "OPENAI_API_KEY=YOUR_KEY\n"
        )

    client = OpenAI(
    api_key=api_key,
    base_url="https://apinet.cloud/v1"
    )

    for job in JOBS:
        out_path = OUT_DIR / job["filename"]
        prompt = _build_prompt(job["prompt_suffix"])

        print(f"Generating: {job['filename']} ({job['size']})")
        res = client.images.generate(
            model="gpt-image-2.5-sunburst",
            prompt=prompt,
            size=job["size"],
            quality="high",
            output_format="png",
        )

        b64 = res.data[0].b64_json
        out_path.write_bytes(base64.b64decode(b64))
        print(f"Saved: {out_path}")


if __name__ == "__main__":
    main()

